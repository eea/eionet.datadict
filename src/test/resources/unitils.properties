# Unitils database configuration for inseting data by DBUnit
database.driverClassName=${test.db.driver}
database.url=${test.db.jdbcurl}
database.userName=${test.db.user}
database.password=${test.db.password}
database.schemaNames=DataDictTest
database.dialect=mysql

DatabaseModule.Transactional.value.default=rollback
dbUnit.datasetresolver.prefixWithPackageName=false
