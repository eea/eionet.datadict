# file for acl permissions
#application.acl.folder=C:/projects/eeasvn/ACL/test/build
file.aclfolder=target/test-classes

# file for localgroups and users
file.localgroups=target/test-classes/test-dd.group

# file for app permissions + descriptions
file.permissions=target/test-classes/dd.prms

#permission in ACL for the administrator
owner.permission=c

#if true, RPC ACL admin methods are initialized
acl.admin=true

#ACL entry for authenticated acces (not belonging to any group)
authenticated.access=authenticated

#ACL entry for anonymous access
anonymous.access=anonymous

#localuser accounts
file.localusers=target/test-classes/users.xml

# component services
componentservices=HelpService
componentservices.HelpService.provider=eionet.help.RemoteService

db.driver=${test.db.driver}
db.url=${test.db.jdbcurl}
db.user=${test.db.user}
db.pwd=${test.db.password}

defaultdoc.permissions=
persistence.provider=
initial.admin=
services.definition.file=target/test-classes/DataDictService.xml