<?xml version="1.0" encoding="UTF-8"?>
<Context path="/datadict" docBase="C:/projects/eionet.datadict/target/datadict">

    <Resource
        name="jdbc/datadict"
        auth="Container"
        type="javax.sql.DataSource"
        maxActive="100"
        maxIdle="30"
        maxWait="10000"
        username="${db.user}"
        password="${db.password}"
        driverClassName="${db.driver}"
        url="${db.jdbcurl.encoded}"
        validationQuery="SELECT 1"
        testOnBorrow="true"
        >
    </Resource>

</Context>
